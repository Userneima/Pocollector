# 小红书采集工具

## 推荐启动方式（低占用）

- 双击桌面快捷方式 `Redbook 轻量启动`
- 或运行：

```bash
start-redbook.cmd
```

说明：默认会走生产模式（`build + start`），相比 `npm run dev` 占用更低。

## 开发模式（改代码时使用）

```bash
npm run dev
```

## 停止服务

```bash
stop-redbook.cmd
```

## 清理缓存与日志

```bash
clean-redbook.cmd
# 或
npm run clean
```
